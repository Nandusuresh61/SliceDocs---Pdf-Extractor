server {
    listen 80;
    server_name api.yourdomain.com; # Replace with your actual domain or EC2 Public IP

    location / {
        proxy_pass http://127.0.0.1:5000; # Assuming backend runs on port 5000
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Real IP headers for Express
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Increase max body size if you are handling large PDF uploads
    client_max_body_size 50M;
}
